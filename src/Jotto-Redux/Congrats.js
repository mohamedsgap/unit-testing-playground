import React from "react";

/**
 * Functional react component for congrats message
 * @funtion
 * @return {JSX.Element} Rendered component (or null if success props is passed)
 */
const Congrats = (props) => {
  return (
    <div>
      <p>jfoidjaf</p>
    </div>
  );
};

export default Congrats;
